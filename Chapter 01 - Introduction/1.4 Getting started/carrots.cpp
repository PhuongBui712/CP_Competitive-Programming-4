#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, p; cin >> n >> p;
    string name;
    for (int i = 0; i < n; i++) {
        cin >> name;
        cin.ignore();
    }
    cout << p;
    return 0;
}